
# Copia este archivo como .env y coloca tus credenciales

# API key de TMDB (v3 o v4). Si usas v4 (Bearer), pega el token que empieza con 'ey...'
TMDB_API_KEY=REEMPLAZA_CON_TU_API_KEY

# Connection string de tu base de datos (elige una y comenta la otra)

# PostgreSQL (recomendado para práctica local)
# DATABASE_URL=postgresql+psycopg2://usuario:password@localhost:5432/midb

# SQL Server (requiere instalar ODBC Driver 18 y pyodbc)
# DATABASE_URL=mssql+pyodbc://usuario:password@localhost:1433/midb?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes
